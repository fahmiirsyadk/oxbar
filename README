ox
________________________________________________________________________________

  A minimal C toolkit for building X11 status bars and widgets.
  No config files. Write C, not config.


[00] Quick Start
________________________________________________________________________________

  #include "ox.h"

  static void clock_update(void *ctx, char *buf, size_t len) {
      (void)ctx;
      time_t t = time(NULL);
      struct tm *tm = localtime(&t);
      snprintf(buf, len, "%02d:%02d:%02d", tm->tm_hour, tm->tm_min, tm->tm_sec);
  }

  static void on_timeout(OxMain *m, double now) {
      OxWidget *w = m->ctx;
      if (now - ox_widget_get_last_update(w) >= ox_widget_get_interval(w)) {
          ox_widget_update(w);
          ox_widget_set_last_update(w, now);
      }
  }

  static void on_event(OxMain *m, XEvent *ev) {
      (void)ev;
      OxWidget *w = m->ctx;
      if (ev->type == Expose) {
          ox_draw_rect(win, 0, 0, 200, 28, "#000000");
          ox_draw_text(win, 8, 18, ox_widget_get_label(w), "#ffffff");
      }
  }

  int main(void) {
      ox_init();

      OxWindow *win = ox_window_new(0, 0, 200, 28);
      ox_window_set_bg(win, "#000000");
      ox_window_show(win);

      OxWidget *w = ox_widget_new("clock", 1.0);
      ox_widget_set_update(w, clock_update, NULL);

      OxMain loop = { .on_event = on_event, .on_timeout = on_timeout, .ctx = w };
      ox_main(&loop);
      ox_cleanup();
      return 0;
  }


[01] Build
________________________________________________________________________________

  make              builds libox.a only
  make examples     builds all examples
  make install      installs libox.a, ox.h, libox.pc

  Dependencies:
    libx11  libxft  libxpm  freetype2  fontconfig
    Optional: libasound (for xmobar volume widget)


[02] Architecture
________________________________________________________________________________

  ox has three layers:

    Widget (state)         Window (X11 surface)      Draw (primitives)
    --------------         --------------------       ----------------
    OxWidget               OxWindow                   ox_draw_rect
    - name                 - X11 window handle        ox_draw_text
    - label (text)         - Xft font                 ox_draw_xpm
    - update callback      - background color         ox_text_width
    - click callback       - strut (dock reservation)
    - async cmd (fork)     - show / hide
    - dirty flag

  Event loop:

    ox_main()
    ----------
    select() blocks on: X11 fd, IPC fd, widget pipe fds
         |
         +-- on_timeout(now)  ->  update widgets, check dirty, redraw
         +-- on_event(ev)     ->  handle Expose, ButtonPress, KeyPress
         +-- on_ipc(msg)      ->  handle external commands

  Widget update flow:

    ox_widget_update(w)
         |
         +-- has cmd?    -> fork()+exec(), read stdout non-blocking
         |                    (pipe fd exposed for select() monitoring)
         |
         +-- has callback?  -> call update(ctx, buf, 256)
                                 compare buf to current label
                                 if different -> dirty = 1


[03] API Reference
________________________________________________________________________________

  References link to source on GitHub (oxbar repo).

  Core
  ----
  ox_init()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L88
  ox_cleanup()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L93
  ox_main(OxMain*)
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/loop.c#L77
  ox_quit(OxMain*)
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/loop.c#L73
  ox_display()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L85
  ox_screen()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L86

  Widget
  ------
  ox_widget_new()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L97
  ox_widget_destroy()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L106
  ox_widget_set_update()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L121
  ox_widget_set_click()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L126
  ox_widget_set_icon()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L130
  ox_widget_set_cmd()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L135
  ox_widget_set_label_text()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L140
  ox_widget_set_colors()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L149
  ox_widget_update()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L251
  ox_widget_do_click()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L266
  ox_widget_get_fd()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L197
  ox_widget_read()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L214
  ox_widget_get_label()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L270
  ox_widget_get_icon()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L271
  ox_widget_get_fg()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L272
  ox_widget_get_bg()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L273
  ox_widget_get_interval()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L274
  ox_widget_get_last_update()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L275
  ox_widget_set_last_update()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L276
  ox_widget_is_dirty()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L277
  ox_widget_clear_dirty()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/widget.c#L278

  Window
  ------
  ox_window_new()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L121
  ox_window_new_floating()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L157
  ox_window_destroy()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L168
  ox_window_set_bg()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L179
  ox_window_set_font()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L184
  ox_window_set_strut()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L189
  ox_window_set_wm_class()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L204
  ox_window_show()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L212
  ox_window_hide()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L218
  ox_window_handle()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L224

  Draw
  ----
  ox_draw_rect()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L234
  ox_draw_text()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L273
  ox_draw_xpm()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L301
  ox_text_width()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/draw.c#L284

  IPC
  ---
  ox_ipc_init()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/ipc.c#L63
  ox_ipc_fd()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/ipc.c#L84
  ox_ipc_send()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/ipc.c#L88
  ox_ipc_recv()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/ipc.c#L111
  ox_ipc_cleanup()
    https://github.com/fahmiirsyadk/oxbar/blob/main/src/ipc.c#L138

  Types (headers)
  ---------------
  OxMain
    https://github.com/fahmiirsyadk/oxbar/blob/main/include/ox.h#L17
  OxWidgetUpdate callback
    https://github.com/fahmiirsyadk/oxbar/blob/main/include/ox.h#L11
  OxWidgetClick callback
    https://github.com/fahmiirsyadk/oxbar/blob/main/include/ox.h#L12
  OxEventFn callback
    https://github.com/fahmiirsyadk/oxbar/blob/main/include/ox.h#L13
  OxTimeoutFn callback
    https://github.com/fahmiirsyadk/oxbar/blob/main/include/ox.h#L14
  OxIpcFn callback
    https://github.com/fahmiirsyadk/oxbar/blob/main/include/ox.h#L15


[04] Widget Guide
________________________________________________________________________________

  Creating a widget:

    OxWidget *w = ox_widget_new("name", interval_seconds);

  Setting update logic — two approaches:

  A) Callback (for reading /proc, sysfs, time, etc.):

    static void my_update(void *ctx, char *buf, size_t len) {
        (void)ctx;
        FILE *f = fopen("/proc/loadavg", "r");
        if (!f) { snprintf(buf, len, "??"); return; }
        float load;
        fscanf(f, "%f", &load);
        fclose(f);
        snprintf(buf, len, "%.1f", load);
    }

    OxWidget *w = ox_widget_new("load", 2.0);
    ox_widget_set_update(w, my_update, NULL);

  B) Shell command (for pactl, amixer, etc.):

    OxWidget *w = ox_widget_new("vol", 5.0);
    ox_widget_set_cmd(w, "amixer get Master | awk -F'[][]' '/%/ {print $2}'");

  The command runs asynchronously (fork+exec, non-blocking pipe).
  When complete, the first line of stdout becomes the widget label.

  Setting click behavior:

    static void on_click(void *ctx) {
        system("wpctl set-mute @DEFAULT_SINK@ toggle");
    }
    ox_widget_set_click(w, on_click);

  Per-widget colors:

    ox_widget_set_colors(w, "#ff0000", NULL);   /* red fg, default bg */

  Static label (no update callback):

    OxWidget *w = ox_widget_new("power", 0);
    ox_widget_set_label_text(w, "off");


[05] Multiple Widgets / Bars
________________________________________________________________________________

  Bars are arrays of widgets. Each widget updates independently on its own
  interval. See examples/simple-bar.c for a complete implementation.

  Bar struct pattern:

    typedef struct {
        OxWindow *win;
        OxWidget *widgets[16];
        int count;
        int height, padding;
        const char *fg, *bg;
    } Bar;

    static void bar_add(Bar *b, OxWidget *w) {
        b->widgets[b->count++] = w;
    }

  Render draws all widgets in a row:

    static void bar_render(Bar *b) {
        int cx = b->padding;
        int cy = b->height / 2;
        for (int i = 0; i < b->count; i++) {
            if (i > 0) {
                ox_draw_text(b->win, cx, cy, " | ", "#555555");
                cx += ox_text_width(b->win, " | ") + b->padding;
            }
            ox_draw_text(b->win, cx, cy,
                ox_widget_get_label(b->widgets[i]), b->fg);
            cx += ox_text_width(b->win,
                ox_widget_get_label(b->widgets[i])) + b->padding;
        }
    }

  Timeout updates widgets, redraws only if dirty:

    static void on_timeout(OxMain *m, double now) {
        Bar *b = m->ctx;
        int any_dirty = 0;
        for (int i = 0; i < b->count; i++) {
            OxWidget *w = b->widgets[i];
            if (ox_widget_get_interval(w) > 0 &&
                now - ox_widget_get_last_update(w) >=
                    ox_widget_get_interval(w)) {
                ox_widget_update(w);
                ox_widget_set_last_update(w, now);
            }
            if (ox_widget_is_dirty(w)) any_dirty = 1;
        }
        if (any_dirty) {
            bar_render(b);
            for (int i = 0; i < b->count; i++)
                ox_widget_clear_dirty(b->widgets[i]);
        }
    }

  Multiple bars (left / center / right):

    Bar left   = { .height=28, .padding=8, .fg="#fff", .bg="#000" };
    Bar center = { .height=28, .padding=8, .fg="#fff", .bg="#000" };
    Bar right  = { .height=28, .padding=8, .fg="#fff", .bg="#000" };

    left.win   = ox_window_new(0,          0, left_w,   h);
    center.win = ox_window_new((sw-cw)/2,   0, center_w, h);
    right.win  = ox_window_new(sw-rw,       0, right_w,  h);


[06] Click Handling
________________________________________________________________________________

  See examples/xmobar.c for a complete implementation with hit-testing.

  The WidgetLayout struct stores bounding boxes computed during render:

    typedef struct { int x, width; } WidgetLayout;

  In your render function, store layout per widget:

    b->layout[i].x = cx;
    b->layout[i].width = ww;

  In on_event, use bar_hit_test to find which widget was clicked:

    static int bar_hit_test(Bar *b, int x) {
        for (int i = 0; i < b->count; i++)
            if (x >= b->layout[i].x &&
                x < b->layout[i].x + b->layout[i].width)
                return i;
        return -1;
    }

    static void on_event(OxMain *m, XEvent *ev) {
        Bar *b = m->ctx;
        if (ev->type == ButtonPress) {
            if (ev->xbutton.window == ox_window_handle(b->win)) {
                int idx = bar_hit_test(b, ev->xbutton.x);
                if (idx >= 0) ox_widget_do_click(b->widgets[idx]);
            }
        }
    }


[07] IPC
________________________________________________________________________________

  IPC lets external scripts send commands to a running ox bar.
  See src/ipc.c for the implementation.

  Socket path: $XDG_RUNTIME_DIR/ox-ipc-<pid>.sock

  Send a command from a script:

    ./xmobar &
    SOCK="/run/user/$(id -u)/ox-ipc-$(pgrep xmobar).sock"
    echo "update-vol" | socat - UNIX-CONNECT:"path=$SOCK"

  Handle commands in the bar:

    static void on_ipc(OxMain *m, const char *msg) {
        if (strcmp(msg, "update-vol") == 0) {
            ox_widget_update(w_vol);
            /* redraw happens automatically if dirty */
        }
    }

    /* in main: */
    ox_ipc_init();
    OxMain loop = {
        .on_ipc = on_ipc,
        .ipc_fd = ox_ipc_fd(),
    };


[08] Code Style
________________________________________________________________________________

  Formatting:
    - 4-space indentation (no tabs)
    - Opening brace on same line for functions and control flow
    - Single blank line between functions
    - No trailing whitespace

  Naming:
    - ox_ prefix for all public API functions
    - snake_case for functions and variables
    - UPPER_CASE for #define constants
    - OxPascalCase for types (OxWidget, OxWindow, OxMain)
    - g_ prefix for file-scope globals (g_dpy, g_screen)

  Callbacks:
    - Always check for NULL before invoking
    - Pass user context as first parameter
    - Return type void for all callbacks

  Memory:
    - strdup() for owned strings, free() in destroy functions
    - calloc() for struct allocation
    - No hidden allocations in draw functions

  Error handling:
    - Return NULL or -1 on failure (no exceptions)
    - Log to stderr for critical errors (perror, fprintf)
    - Graceful degradation (show "??%" instead of crashing)

  Headers:
    - _POSIX_C_SOURCE 200809L for getline, clock_gettime, etc.
    - _GNU_SOURCE for strcasestr (search example only)
    - Include ox.h last among local headers


[09] Extending
________________________________________________________________________________

  Adding a new widget type:

    1. Write an update callback:

      static void net_update(void *ctx, char *buf, size_t len) {
          FILE *f = fopen("/proc/net/dev", "r");
          if (!f) { snprintf(buf, len, "??"); return; }
          /* parse and format */
          fclose(f);
      }

    2. Create and configure:

      OxWidget *w = ox_widget_new("net", 3.0);
      ox_widget_set_update(w, net_update, NULL);
      ox_widget_set_colors(w, "#00ff00", NULL);

    3. Add to your bar:

      bar_add(&right, w);

  The event loop handles timing, dirty tracking, and rendering.

  Adding a draw primitive:

    Keep it in src/draw.c. Follow the pattern:

      void ox_draw_foo(OxWindow *win, int x, int y /* params */) {
          /* use win->dpy, win->gc, win->draw for X11 operations */
      }

    Add the declaration to include/ox.h under the Draw section.

  Adding an event type:

    Add a callback field to OxMain (include/ox.h:17),
    wire it in ox_main()'s select() loop (src/loop.c:77),
    and expose via the API.


[10] Examples
________________________________________________________________________________

  example        description
  -------        -----------
  simple-bar     full-width bar: time, cpu, mem
  multi-bar      three bars: left/center/right panels
  vertical-bar   vertical sidebar with stacked widgets
  osd            on-screen display (volume, brightness)
  xmobar         full status bar: ALSA vol, battery, clock, workspaces
  search         fuzzy search launcher (dmenu-like)

  Source: examples/*.c


[11] Signals
________________________________________________________________________________

  signal    action
  ------    ------
  SIGINT    exit event loop cleanly
  SIGTERM   exit event loop cleanly
  SIGCHLD   ignored (child processes auto-reap)


[12] License
________________________________________________________________________________

  MIT
